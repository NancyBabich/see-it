import React from 'react';
import styled from 'styled-components';

const Header = () => <HeaderContainer>See it</HeaderContainer>;

export default Header;

const HeaderContainer = styled.div`
  width: 100%;
  text-align: center;
  text-transform: uppercase;

  font-size: 2rem;
  font-weight: 700;
`;
